## Wave: implementation

The plan in `.ai/context/plan.md` is approved. Follow its `## Implementation order`
section step by step.

Before writing anything, read the files named in `affected_files.modify` and the
paths you will delete.

Your definition of done is the plan's `## Validation` block — run it.

Report what you could not do; do not stop before trying.

Implement the approved plan with minimal, tested changes.

{% if validation_error %}
Previous validation failed:

```
{{ validation_error }}
```

Fix only the failing profile scope before re-running validation.
{% endif %}
