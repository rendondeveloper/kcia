# Workflow: refactor

1. Ensure existing test coverage before refactoring.
2. Small, incremental changes; one logical commit per step.
3. Don't mix refactoring with behavior changes.
4. Run `verify` after each significant step.
