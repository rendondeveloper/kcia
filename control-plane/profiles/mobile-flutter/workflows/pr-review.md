# Workflow: PR review (mobile)

1. Verify accessibility (`Semantics`) on new screens.
2. Check that commands use `fvm flutter` when the project requires it.
3. Review permissions, deep links, and platform channels if applicable.
