# Workflow: local skills

Register project skills with `kcia skill --backend --path .cursor/skills/<name>` and run them with `kcia skill --backend --<shortcut>` (for example `kcia skill --backend --deploy`).
